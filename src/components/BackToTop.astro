---
---

<button
    id="back-to-top"
    class="fixed bottom-8 right-8 z-50 bg-primary hover:bg-primary-dark text-white w-10 h-10 rounded-full shadow-lg transform translate-y-24 opacity-0 transition-all duration-300 flex items-center justify-center"
    aria-label="Back to top"
>
    <span class="text-2xl leading-none" aria-hidden="true">↑</span>
</button>

<script>
    const backToTopButton = document.getElementById('back-to-top');

    // Show/hide button based on scroll position
    const toggleBackToTop = () => {
        if (window.scrollY > 300) {
            backToTopButton?.classList.remove('translate-y-24', 'opacity-0');
        } else {
            backToTopButton?.classList.add('translate-y-24', 'opacity-0');
        }
    };

    // Smooth scroll to top
    backToTopButton?.addEventListener('click', () => {
        window.scrollTo({
            top: 0,
            behavior: 'smooth'
        });
    });

    // Add scroll event listener with throttling
    let ticking = false;
    window.addEventListener('scroll', () => {
        if (!ticking) {
            window.requestAnimationFrame(() => {
                toggleBackToTop();
                ticking = false;
            });
            ticking = true;
        }
    });

    // Initial check
    toggleBackToTop();
</script>
